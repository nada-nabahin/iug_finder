import 'package:flutter/material.dart';

class CustomCardReport extends StatelessWidget {
  const CustomCardReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(),
    );
  }
}
